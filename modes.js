let carouselMode='ring',phase=0,last=performance.now();const cards=[...document.querySelectorAll('.work')];function modeFrame(t){const dt=Math.min(40,t-last);last=t;if(carouselMode==='tilt'||carouselMode==='gallery'){phase=(phase+dt*.00042)%(Math.PI*2);const n=cards.length;cards.forEach((el,i)=>{const a=((i/n)*Math.PI*2+phase);const s=carouselMode==='gallery'?(.62+.38*Math.max(0,Math.cos(a))):(.72+.28*Math.max(0,Math.cos(a)));const x=carouselMode==='gallery'?Math.sin(a)*43:Math.sin(a)*57;const y=carouselMode==='gallery'?Math.cos(a)*8:Math.cos(a)*4;const z=carouselMode==='gallery'?Math.max(0,Math.cos(a)):Math.max(0,Math.cos(a));el.style.left='50%';el.style.top=`calc(50% + ${y}%)`;el.style.transform=`translate(-50%,-50%) translateX(${x}vw) scale(${s}) rotate(${Math.sin(a)*3}deg)`;el.style.opacity=carouselMode==='gallery'?(0.34+z*.66):(0.5+z*.5);el.style.zIndex=Math.round(z*30)})}requestAnimationFrame(modeFrame)}requestAnimationFrame(modeFrame);document.querySelectorAll('nav button').forEach(b=>b.addEventListener('click',()=>{carouselMode=b.dataset.mode;if(carouselMode==='tilt'||carouselMode==='gallery'){stage.classList.remove('mode-ring');stage.classList.add(`mode-${carouselMode}`)} }));cards.forEach(c=>c.addEventListener('click',e=>{if(carouselMode==='tilt'||carouselMode==='gallery'){c.classList.remove('ripple');void c.offsetWidth;c.classList.add('ripple');}}));
